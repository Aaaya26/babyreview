<h2 class="sign-title">Edit account</h2>

<%= form_with model: current_user, class: "edit-user-main", local: true do |f| %>

  <div class="form-group">
    <div class="form-text-wrap">
      <label class="form-text">名前</label>
    </div>
    <%= f.text_area :name, class:"input-default", id:"name", placeholder:"10文字以内で入力してください", maxlength:"10" %>
  </div>

  <div class="form-group">
    <div class="form-text-wrap">
      <label class="form-text">メールアドレス</label>
    </div>
    <%= f.email_field :email, class:"input-default", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true %>
  </div>

  <div class="form-group">
    <div class="form-text-wrap">
      <label class="form-text">パスワード</label>
      <span class="indispensable">変更する場合のみ入力</span>
    </div>
    <%= f.password_field :password,  class:"input-default", id:"password", placeholder:"6文字以上の半角英数字" %>
  </div>

  <div class="form-group">
    <div class="form-text-wrap">
      <label class="form-text">確認用パスワード</label>
    </div>
    <%= f.password_field :password_confirmation,class:"input-default", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい" %>
  </div>

  <div class="form-group">
    <div class='form-text-wrap'>
      <label class="form-text">生年月日</label>
    </div>
    <div class='input-birth-wrap'>
      この情報は変更できません
    </div>
  </div>

  <div class="form-group">
    <div class='form-text-wrap'>
      <label class="form-text">お子様の生年月日</label>
      <span class="indispensable">任意</span>
    </div>
    <div class='input-birth-wrap'>
      <%= raw sprintf(
                f.date_select(
                  :child_birthday,
                  class:'select-birth',
                  id:"birth-date",
                  use_month_numbers: true,
                  prompt:'--',
                  start_year: 2015,
                  end_year: (Time.now.year),
                  date_separator: '%s'),
                "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit "Edit account!", class:"registration-btn" %>
    <div class="btn">
      <%=link_to 'back', user_path(current_user), class:"back-btn" %>
    </div>
  </div>

<% end %>

